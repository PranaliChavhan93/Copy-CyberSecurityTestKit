services:
  frontend:
    build:
      context: ./frontend

    container_name: cybertoolkit-frontend

    ports:
      - "5173:5173"

    volumes:
      - ./frontend:/app
      - /app/node_modules

    stdin_open: true
    tty: true